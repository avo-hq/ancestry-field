<%= show_field_wrapper field: @field, index: @index do %>
  <ul>
    <% @field.value.all.each do |p| %>
      <li><%= link_to p.name, "/admin/resources/people/#{p.id}" %></li>
    <% end %>
  </ul>
<% end %>
